<UserControl x:Class="WpfProj.View.WidowSetting"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfProj.View"
             xmlns:converter="clr-namespace:WpfProj.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:NameConverter x:Key="nameConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition/>            
        </Grid.ColumnDefinitions>-->
        <Slider Grid.Row="0" Value="{Binding Width}" Minimum="1" Maximum="100" />
        <Label  Grid.Row="1" >Width</Label>
        <TextBlock Grid.Row="1" Text="{Binding Width}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <TextBlock Grid.Row="2" Name="textBox2" DataContext="{Binding NameList}">
            <TextBlock.Text>
                    <MultiBinding Converter="{StaticResource nameConverter}" ConverterParameter="FormatLastFirst">
                    <Binding Path="FirstName"/>
                    <Binding Path="LastName"/>
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
    </Grid>
</UserControl>
